CI CONTROL ARTEFACT - DELETE WITH THE BRANCH. NOT A CREDENTIAL.

This file exists to make the `Scan tracked files against baseline` step of the
`Secret scan` job fail on demand, so that the step which follows it can be
observed. It is committed only to a scratch branch that is never merged.

The string at the bottom is 48 random alphanumeric characters generated locally
with Python's `secrets` module. It authenticates nothing, was never issued by
any system, and has no referent anywhere. Its only relevant property is Shannon
entropy over the base64 charset - measured locally at 5.0952 against
detect-secrets 1.5.0's Base64HighEntropyString limit of 4.5, using that plugin's
own formula, with a repeated-character negative control measuring 0.0000 so an
all-TRIPS reading could not have come from a broken probe.

This file deliberately contains NO http or https address, so that the
`URL host allowlist` step's own conclusion in the same run is attributable and
is not perturbed by the control itself.

CONTROL-MARKER-SCANFAIL-BRANCH

password = YlVkBuW2wKF6tiMglsJdLXGbzlxTv7pzPKekoRdURzW682jf
