# FreeTier Atlas — local development stack.
#
# Slice 1 of the F002 scaffold brings up PostgreSQL and the FastAPI API. The
# web frontend, worker, and scheduler services are added in slice 2 (see the
# commented block at the end of this file).
#
# Values are sourced from a local .env file (copy .env.example -> .env). The
# defaults are non-secret local development values and MUST NOT be used in any
# deployed environment.

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-atlas}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-atlas}
      POSTGRES_DB: ${POSTGRES_DB:-atlas}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - atlas_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-atlas} -d ${POSTGRES_DB:-atlas}"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+psycopg://atlas:atlas@postgres:5432/atlas} # pragma: allowlist secret
      APP_ENV: ${APP_ENV:-development}
    ports:
      - "${API_PORT:-8000}:8000"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test:
        - "CMD-SHELL"
        - 'python -c "import urllib.request,sys; sys.exit(0) if urllib.request.urlopen(''http://localhost:8000/health'', timeout=3).status==200 else sys.exit(1)"'
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 20s
    restart: unless-stopped

volumes:
  atlas_pgdata:
# --- Deferred to F002 slice 2 (do not enable without an owner checkpoint) ---
#   web:
#     build:
#       context: .
#       dockerfile: apps/web/Dockerfile
#     depends_on:
#       api:
#         condition: service_healthy
#   worker:
#     build:
#       context: .
#       dockerfile: apps/worker/Dockerfile
#     depends_on:
#       postgres:
#         condition: service_healthy
#   scheduler:
#     build:
#       context: .
#       dockerfile: apps/worker/Dockerfile
#     command: ["python", "-m", "worker.scheduler"]
#     depends_on:
#       postgres:
#         condition: service_healthy
