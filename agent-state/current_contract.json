{
  "schema_version": 1,
  "status": "approved",
  "feature_id": "F006",
  "increment": "slice-4-adviser-recommendation-web-experience",
  "builder": "Copilot CLI (Chief)",
  "evaluator": "fresh independent Level 2 evaluator (per-slice), then a later full-epic Level 2 close-out owns the passes flip",
  "approved_at": "2026-07-24T00:00:00Z",
  "approval_notes": [
    "Orchestrator-directed F006 Slice 4 (the FINAL slice of the catalogue-and-adviser epic): the ADVISER WEB EXPERIENCE. Build ONE slice, open ONE PR, do NOT merge, do NOT flip any passes flag. F006 stays passes:false.",
    "Adds a new hash route (#/adviser) to the existing apps/web SPA that presents an editable STRUCTURED requirements form (components/services + quota demands with explicit units), submits it to the existing POST /api/adviser/recommend via the same-origin /api proxy, and renders the satisfiable recommendation ($0 architecture + per-component quota math/headroom + Z0-safety reasons + portability/lock-in/exit-plan + whole-architecture $0 proof) and the impossible-workload flow in the STRICT order the API returns (1. blocking -> 2. reduction -> 3. recalculation -> 4. self-hosting), with Z1/Z2 only in a clearly separated 'not $0' section.",
    "NO natural-language input, NO LLM, NO consent flow, NO ZIP export (all deferred to F007). The page is a plain structured form only.",
    "Consumes ONLY /api/adviser/recommend (POST) plus the existing /api/catalogue GETs already used by the SPA. Adds exactly ONE typed fetcher (fetchRecommendation). No new backend endpoint, no DB access, no write/mutation beyond the stateless adviser POST.",
    "Standing autonomy boundaries: may branch/implement/test/commit/push/open ONE PR; may NOT merge, flip any passes flag, add an npm or Python dependency, add a migration (head stays 0007), touch backend source (pytest count stays 556/61), add a natural-language/LLM/consent/ZIP path, or emit a fabricated real-world free claim (a live stack shows only real published Cloudflare data; synthetic provider names live only in test files)."
  ],
  "objective": "Add an adviser recommendation web experience to the existing provider-agnostic SPA: a new #/adviser hash route rendering an editable, keyboard-operable STRUCTURED requirements form (workload name + a bounded list of requirements, each with a canonical category, optional label, one-or-more quantified demands (metric + exact amount + unit + optional period), and optional constraints (commercial use, personal use, region, residency)). On submit it POSTs the structured requirements to the existing /api/adviser/recommend through the same-origin /api proxy via ONE new typed fetcher, then renders the DETERMINISTIC recommendation exactly as the API returns it: when satisfiable, the recommended Z0 architecture with per-component selected offer/provider, exact per-component quota math + headroom, Z0-safety reasons (display only, never re-derived), portability/lock-in/exit-plan, and the whole-architecture $0 proof; when a requirement is blocking, the impossible flow in the STRICT order 1. blocking conditions -> 2. reduction -> 3. recalculation -> 4. self-hosting; and Z1/Z2 (not-$0) options only in a clearly separated 'Not $0 / paid' section. Honest 'Unknown' for any null/absent field; confidence LABEL primary with any numeric only inside a CLOSED nested <details>; the UI never re-derives Z0 class, confidence, or quota math.",
  "scope": [
    "apps/web/src/api.ts: ADD the RecommendationRequest/RecommendationResponse TypeScript types (mirroring apps/api/app/adviser/schemas.py + schema.py) and ONE typed fetcher fetchRecommendation(request, signal) that POSTs JSON to the FIXED /adviser/recommend path via a small postJson helper (Accept + Content-Type application/json). No URL/host is ever taken from the caller; the body is structured requirements only.",
    "apps/web/src/adviser/vocab.ts (new): the 14 canonical category options (slug + display name, mirroring app.read_api.taxonomy) plus advisory metric/unit/period suggestion lists for <datalist>s. Labels only; the UI never derives cost/Z0 from them.",
    "apps/web/src/adviser/AdviserForm.tsx (new): the editable structured requirements <form> -- labelled workload-name input; a repeatable requirement group (category <select>, optional label, add/remove demand rows each with labelled metric/amount/unit/optional period inputs, and constraint checkboxes + optional region/residency inputs); add/remove requirement controls; client-side validation (>=1 requirement, each with >=1 complete demand and a positive amount) with accessible inline messages; emits a typed RecommendationRequest on submit.",
    "apps/web/src/adviser/RecommendationView.tsx (new): renders a RecommendationResponse -- an overall summary banner (fully-$0 badge: colour + visible text + aria-hidden icon), the priorities, the whole-architecture $0 proof, the recommended architecture components (accessible per-component detail incl. a quota-math table with caption + th scope, Z0-safety reasons, portability with numeric score only inside a CLOSED <details>, exit plan, evidence links rel=noopener noreferrer), the impossible resolutions in the API's strict order (1 blocking -> 2 reduction -> 3 recalculation -> 4 self-hosting), and the separate clearly-marked 'Not $0 / paid' section. Honest 'Unknown' for null fields; never re-derives anything.",
    "apps/web/src/App.tsx: add the #/adviser route to the hash router + a primary-nav link + an AdviserView container (owns the POST request state: idle form -> loading -> ok/render or accessible error with retry). Single <h1> per route preserved.",
    "apps/web/src/App.css: additive adviser styles (form layout, requirement/demand rows, recommendation cards, impossible-step list, not-free section) reusing the existing badge/card/table/details tokens; no colour-only signalling.",
    "apps/web/src/adviser/testFixtures.ts (new): clearly-SYNTHETIC multi-provider recommendation fixtures (satisfiable, mixed/impossible with the full reduction->recalculation->self-hosting order, and a Z1/Z2 not-free section) used ONLY by tests -- synthetic provider names never appear in production components/api.ts.",
    "apps/web/src/adviser/adviser.test.tsx (new): offline deterministic vitest (jsdom, fireEvent NOT user-event) for the form (labels, add/remove requirement+demand, validation, typed-submit payload) and RecommendationView (satisfiable $0 architecture render incl. quota-math table + $0 proof; impossible strict order 1->2->3->4; Z1/Z2 only in the separate section; honest Unknown; confidence numeric only in a CLOSED <details>; multi-provider render; accessible tables + labelled controls + rel=noopener external links).",
    "apps/web/src/api.test.ts: ADD a fetchRecommendation block asserting it POSTs to /api/adviser/recommend with the JSON body + application/json headers and parses the response (mocked fetch; no network).",
    "apps/web/src/App.test.tsx: ADD an adviser-route block -- navigating to #/adviser renders a single <h1>, the structured form, submits it, and renders the recommendation from a mocked /api/adviser/recommend (satisfiable + impossible), asserting the nav landmark/aria-current and no NL/LLM/consent/ZIP affordances exist.",
    "docs/ARCHITECTURE.md: add an F006 slice-4 adviser-web subsection (structured form, single POST fetcher, display-only rendering, strict impossible order, honest Unknown, provider-agnostic via synthetic fixtures). agent-state/progress.md: append a builder handoff. agent-state/current_contract.json: this file (per-slice contract)."
  ],
  "out_of_scope": [
    "Any natural-language / free-text project description input, LLM, 'chat with your data', external-model consent flow, or browser ZIP export (all F007).",
    "Any new backend endpoint, backend source change, or DB access; any write/mutation beyond the stateless adviser POST.",
    "Any new npm or Python runtime dependency (requirements-sync + npm audit stay green); any test util beyond the already-present React + fireEvent.",
    "Any migration (Alembic head stays 0007); the backend pytest count stays 556/61 unchanged.",
    "Re-deriving Z0 class, confidence, or quota math in the UI (display only); guessing any null field (honest 'Unknown').",
    "Any user-controlled URL / fetchable host (the only POST target is the FIXED same-origin /api/adviser/recommend path).",
    "Fabricating a real-world free claim on a live stack (synthetic provider names live only in test files); flipping F006 passes:true or editing any feature_list.json passes flag."
  ],
  "acceptance_criteria": [
    "1. A new #/adviser route in the existing SPA presents an editable, keyboard-operable STRUCTURED requirements form (workload name + repeatable requirements with canonical category, optional label, add/remove quantified demands with explicit units, and optional constraints). No natural-language input, no LLM, no consent flow, no ZIP export exists on the page.",
    "2. Submitting the form POSTs the structured requirements to the existing /api/adviser/recommend through the same-origin /api proxy via exactly ONE new typed fetcher (fetchRecommendation); no other new backend call and no new backend endpoint.",
    "3. A satisfiable workload renders the recommended Z0 architecture: per component the selected offer/provider, exact per-component quota math + headroom (as returned), Z0-safety reasons (display only), portability/lock-in/exit-plan, and the whole-architecture $0 proof exactly as the API returns it.",
    "4. An impossible workload renders in the STRICT API order: 1. blocking conditions -> 2. reduction -> 3. recalculation -> 4. self-hosting. Z1/Z2 (not-$0) options appear ONLY in a clearly separated 'Not $0 / paid' section, never mixed into the $0 architecture.",
    "5. Honest 'Unknown' for any null/absent API field (never guessed). The UI never re-derives Z0 class, confidence, or quota math. Confidence: plain-language LABEL primary; any numeric score only inside a CLOSED nested <details>.",
    "6. a11y: single <h1> per route; semantic landmarks + primary nav with aria-current; ordered headings; keyboard-operable form controls + <details>; every form input has an associated <label>; result tables use caption + th scope; external links use rel=noopener noreferrer; every badge pairs colour + visible text + aria-hidden icon (never colour-only). Asserted in tests.",
    "7. Provider-agnostic multi-provider rendering is proven via clearly-SYNTHETIC fixtures in test files only (synthetic provider names never appear in production components/api.ts); a live stack shows only real published Cloudflare data.",
    "8. No new npm/Python dependency and no migration: Alembic head stays 0007; requirements-sync + npm audit stay green; the backend pytest count stays 556/61 unchanged (backend source untouched).",
    "9. Offline gates green: scripts/test.ps1 exit 0 (pytest 556/61 unchanged + web vitest incl. the new adviser tests + vite build) and scripts/check.ps1 -NodeAudit exit 0 (all 8 gates).",
    "10. Live (alt ports, isolated compose project): fresh stack (migrations 0001..0007, head 0007) + real Cloudflare publish + smoke; a headless jsdom render of the REAL <App/> against the live /api proxy plus a raw POST /api/adviser/recommend curl cross-check, for BOTH a satisfiable and an impossible workload, renders the deterministic recommendation and the strict reduction->recalculation->self-hosting order with LLM disabled.",
    "11. F006 remains passes:false; a fresh-context independent Level 2 evaluator owns any status change."
  ],
  "verification_plan": {
    "baseline_commands": ["git status --short --branch", "git --no-pager log --oneline -20", "pwsh -File scripts/test.ps1", "pwsh -File scripts/check.ps1 -NodeAudit"],
    "unit_commands": ["pwsh -File scripts/test.ps1", "pwsh -File scripts/check.ps1 -NodeAudit"],
    "integration_commands": [
      "Fresh live stack on alternate host ports (POSTGRES_PORT=55432, API_PORT=8010, WEB_PORT=8090; DATABASE_URL host-side seed only, NOT exported to compose), isolated compose project -p ftatlas_s4: docker compose ... down --volumes -> scripts/stack-up.ps1 --build (fresh DB applies 0001-0007, head 0007) -> python -m app.ingest.runner cloudflare.example.yaml --fixtures tests/fixtures/ingest/cloudflare/html --publish -> scripts/stack-smoke.ps1 -> drive the adviser page (headless jsdom render of the REAL <App/> against the live /api proxy on :8090) plus a raw POST /api/adviser/recommend curl cross-check, for BOTH a satisfiable and an impossible workload, confirming the deterministic recommendation and the strict blocking->reduction->recalculation->self-hosting order with LLM disabled -> docker compose ... down --volumes. Delete scratch; leave git clean."
    ]
  },
  "risks": {
    "data_migration": "None: Slice 4 is web-only. No migration; Alembic head stays 0007; no table; the consumed endpoint is stateless.",
    "security_privacy": "The page issues a single same-origin POST to the FIXED /api/adviser/recommend path with a structured-requirements body; no URL/host is taken from the user and fetched (no SSRF surface). No secrets, no auth, no persistence, no logging of the body. No natural-language/LLM/consent/ZIP path is added.",
    "portability": "No new npm or Python runtime dependency; reuses React + the existing test utils already present. amd64/arm64 unaffected.",
    "zero_cost_safety": "The UI is display-only: it renders the deterministic recommendation exactly as the API returns it and never re-derives Z0 class, confidence, or quota math. Z1/Z2 (not-$0) options are rendered only in a clearly separated section, never inside the $0 architecture. Null/absent fields render as honest 'Unknown'. On a live stack only real published Cloudflare data appears; synthetic multi-provider data lives only in test files.",
    "immutability": "Web-only slice: no INSERT/UPDATE/DELETE and no DB access from the app. Backend source (including immutability/separation triggers) is untouched.",
    "regression": "Additive TS types + one additive POST fetcher + one additive route/nav link + a new adviser component directory + additive CSS + additive tests. The existing catalogue views, fetchers, and all existing tests are unchanged; backend untouched (pytest 556/61).",
    "supply_chain": "No new npm or Python dependency; requirements-sync + npm audit stay green; already-present deps only."
  },
  "required_evaluation_level": 2,
  "contract_review": {
    "required": true,
    "reviewer": "owner",
    "disposition": "approved",
    "notes": ["Orchestrator directed F006 Slice 4 (FINAL slice): the adviser recommendation web experience, then STOP for a fresh-context Level 2 evaluation. Do NOT flip passes; do NOT merge; do NOT add a migration or a dependency; structured form only (no NL/LLM/consent/ZIP); consumes only the existing adviser POST + catalogue GETs; display-only (never re-derive Z0/confidence/quota math); honest Unknown; strict impossible order blocking->reduction->recalculation->self-hosting with Z1/Z2 only in the separate section; a11y is part of done; provider-agnostic proven via synthetic test-only fixtures."]
  }
}
