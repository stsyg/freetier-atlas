{
  "feature": "vercel-sidecar-sha256-original-disclosure",
  "increment": "Close the disclosure gap on 8 Vercel capture sidecars + add a durable guard",
  "acceptance": [
    "All 8 Vercel capture.json with a non-null sha256_original carry a sha256_original_note",
    "Note wording is evidence-grounded (per-build nonce, evidenced in-repo) and matches the cross-provider convention (contains 'not reproducible across fetches')",
    "Each note uses that file's own fetched_at date",
    "Durable guard: any sidecar with non-null sha256_original must carry a sha256_original_note; derived from glob; non-vacuous count + at-least-one-real-digest asserted",
    "Null-digest sidecars (cloudflare) do NOT require a note",
    "No capture bytes changed (source.html/expected.json); only sidecar JSON + test file",
    "Full suite + ruff green; offline invariant untouched"
  ]
}
