{
  "schema_version": 1,
  "status": "approved",
  "feature_id": "F007",
  "increment": "slice-5a-negative-security-adversarial-regression-corpus",
  "builder": "Copilot CLI (Chief)",
  "evaluator": "fresh independent Level 2 evaluator (per-slice); a later full-epic Level 2 close-out owns the passes flip",
  "approved_at": "2026-07-30T00:00:00Z",
  "base_commit": "ca1ca0e",
  "approval_notes": [
    "Orchestrator-directed F007 Slice 5a: author a PERMANENT, cross-cutting NEGATIVE-SECURITY adversarial regression corpus (committed tests only) that consolidates and EXTENDS the security guarantees of the four already-merged F007 slices (S1 LLM intake, S2 abuse controls, S3 ZIP export, S4 admin) into one durable suite so future changes cannot silently regress them. Maps to F007 acceptance step 4 (negative security + audit) and reinforces steps 1-3. Build ONE slice, open ONE PR, do NOT merge, do NOT flip any passes flag. F007 stays passes:false.",
    "HARD CONSTRAINTS: NO migration (alembic head stays 0009_admin_audit); NO new runtime OR test dependency (FastAPI TestClient + pytest + existing fakes/mocks only); NO Redis; NO real network (no github.com, no real LLM provider); Q1 (fake adapter only) + N1 (OAuth/CAPTCHA mocked) honoured; no secrets in source/tests/examples (planted-secret fixtures fragment-assembled so detect-secrets stays green on tracked files)."
  ],
  "objective": "Add a self-contained, fully-offline/deterministic negative-security regression corpus under tests/security/ that asserts the F007 security invariants across all four surfaces, adding coverage for any invariant not already exercised and consolidating/strengthening the partial ones. All tests run via the real FastAPI app (TestClient) or the pure functions with dependency-injected fakes/in-memory stores and monkeypatched router seams - no conftest, no database, no network. The corpus is durable documentation-as-tests: each invariant is asserted so a future regression fails loudly.",
  "scope": [
    "tests/security/__init__.py (NEW): package marker/docstring.",
    "tests/security/test_security_corpus_llm_intake.py (NEW, S1): user-URL/SSRF in description -> 422; malformed/oversized/unknown-field/wrong-type/URL-smuggled LLM output rejected by the strict schema -> deterministic fallback (invalid_interpretation); provider timeout/error -> graceful degrade; a valid LLM interpretation IS used; deterministic fallback is byte-identical to /recommend with providers disabled; NO LLM-to-publication path (spy session records zero add/commit/flush on assisted + structured paths, even on the llm_used=True path); consent is ephemeral (no consent/user-description column on Base.metadata + spy-session no-write with consent=true); prompts/descriptions are NEVER logged (caplog canary); a subprocess import-guard proves the deterministic core imports no LLM adapter.",
    "tests/security/test_security_corpus_abuse.py (NEW, S2): deterministic overage -> 429 + Retry-After (service + HTTP); ONLY hashed IP persisted end-to-end (scan store rate keys + PoW ip fields are 64-hex, never a dotted-quad/colon; dedupe key is hex not raw body); AI kill switch ON -> assisted degrades to deterministic (200, llm_used=false) WITHOUT hard-fail while /recommend stays 200; per-provider circuit breaker opens after N failures adding ZERO extra provider calls, half-open closes on success and reopens on failure; request dedupe collapses identical requests (service + HTTP fake.calls==1); PoW is server-signed (stdlib hmac), single-use, expiring, ip-bound, constant-time verified, and forged/replayed/expired/downgraded/insufficient-work/malformed tokens are ALL rejected.",
    "tests/security/test_security_corpus_export.py (NEW, S3): fail-closed validators reject path traversal (absolute / .. / backslash), non-allowlisted paths, invalid YAML, missing/empty healthcheck, missing multi-arch (amd64+arm64) assertion, oversized single file + oversized total, NUL/control bytes, planted secrets (AWS/GitHub/Slack/OpenAI/GCP/PRIVATE KEY/keyword-assigned, all fragment-assembled), missing compose, duplicate path; the produced bundle is secret-free; build_export persists nothing (open spy); the router maps ExportValidationError -> 422.",
    "tests/security/test_security_corpus_admin.py (NEW, S4): a non-allowlisted GitHub login -> 403 + NO session cookie + audited; UNAUTHENTICATED access to EVERY guarded admin function -> 401 (valid bodies so the body validator never masks the auth check) + audited; a forged/tampered/expired/wrong-key signed cookie -> 401 across EVERY guarded endpoint; missing/invalid CSRF on EVERY mutation -> 403; missing/mismatched/forged OAuth state -> 401 (incl. a matching-but-signature-invalid state); missing code -> 400; a failed OAuth exchange -> 401; a deep no-leak scan across ALL audit rows (success + denied) proves no signing key / client secret / client id / raw OAuth code / access token appears in any row's actor, reason, or context (keys or values)."
  ],
  "out_of_scope": [
    "Any migration or schema change (alembic head stays 0009_admin_audit).",
    "Any new runtime or test dependency; any Redis; any real github.com or real LLM-provider network.",
    "Refactoring or behaviour changes to the S1-S4 source beyond a MINIMAL, prominently-called-out hardening fix landed ALONGSIDE its own regression test IF a genuine security gap is found (none was found; this slice is tests-only).",
    "Editing feature_list.json (F007 passes stays false), evaluation.json, or any other feature's records; flipping any passes flag; merging the PR."
  ],
  "acceptance_criteria": [
    "1. S1 (LLM/intake): the corpus asserts no LLM-to-publication path, user-URL/SSRF rejection (422), strict-schema fail-closed to deterministic fallback on malformed/oversized/unknown-field output, ephemeral+never-persisted consent, never-logged prompts/descriptions, byte-identical deterministic fallback, and a no-LLM-import guard on the deterministic core.",
    "2. S2 (abuse): the corpus asserts deterministic overage -> 429 + Retry-After, hashed-IP-only persistence, kill-switch graceful degrade with /recommend still up, breaker open/zero-extra-calls/half-open close+reopen, dedupe collapse, and PoW server-signed/single-use/expiring/ip-bound/constant-time with forged/replayed/expired/downgraded/insufficient/malformed all rejected.",
    "3. S3 (export): the corpus asserts fail-closed rejection of traversal/non-allowlist/invalid-YAML/missing-or-empty-healthcheck/missing-multi-arch/oversized/NUL-control/planted-secret/missing-compose/duplicate-path, a secret-free output, and build_export persisting nothing.",
    "4. S4 (admin): the corpus asserts non-allowlisted -> 403 no session + audited, unauthenticated -> 401 on every function + audited, forged/tampered/expired/wrong-key cookie -> 401 on every endpoint, missing/invalid CSRF -> 403 on every mutation, missing/mismatched/forged/signature-invalid state -> 401, missing code -> 400, failed exchange -> 401, and a no-leak scan of every audit row (success + denied).",
    "5. Offline gates green: scripts/test.ps1 exit 0 (pytest incl. all new security tests + web vitest 103 + vite build) with the pytest total exceeding the prior 728 passed / 63 skipped by the added tests; scripts/check.ps1 -NodeAudit exit 0 (all 8 gates, detect-secrets green on tracked files re-run AFTER git add).",
    "6. HARD CONSTRAINTS held: no migration (alembic head 0009_admin_audit), no new runtime/test dependency, no Redis, no real network, no secrets committed (planted fixtures fragment-assembled), existing behaviour unchanged (tests-only).",
    "7. F007 remains passes:false; a fresh-context independent Level 2 evaluator owns any status change. Did NOT merge; did NOT flip passes; did NOT touch feature_list.json / evaluation.json."
  ],
  "verification_plan": {
    "baseline_commands": ["git status --short --branch", "git --no-pager log --oneline -20", "pwsh -File scripts/test.ps1", "pwsh -File scripts/check.ps1 -NodeAudit"],
    "unit_commands": ["pwsh -File scripts/test.ps1", "pwsh -File scripts/check.ps1 -NodeAudit"],
    "integration_commands": [
      "None required: the corpus is fully offline (TestClient + pure functions + in-memory stores + DI fakes + monkeypatched seams); no live stack, no database, and no network are needed. The already-merged S1-S4 slices retain their own live evidence."
    ]
  },
  "risks": {
    "data_migration": "None: no migration, no schema change; alembic head stays 0009_admin_audit.",
    "security_privacy": "This slice STRENGTHENS the security posture by pinning the F007 negative-security invariants as durable regression tests (SSRF/URL rejection, fail-closed schema/export validators, hashed-IP-only, kill-switch/breaker/dedupe/PoW, admin authn+CSRF+OAuth-state, audit no-leak). Planted-secret test fixtures are assembled from fragments at runtime so no contiguous credential literal is committed and detect-secrets stays green on tracked files.",
    "portability": "No new runtime or test dependency and no Redis: the corpus uses only pytest + FastAPI TestClient + the Python stdlib + the repo's existing fakes/in-memory stores. amd64/arm64 unaffected.",
    "zero_cost_safety": "The corpus asserts (does not weaken) the guarantees that keep the subsystem safe: no LLM-to-publication path, deterministic byte-identical fallback, fail-closed export, and admin gating - reinforcing Z0-safety rather than touching classification/quota logic.",
    "immutability": "Additive test-only change; no source, model, migration, or config touched (tests-only). Existing immutability/separation guarantees are unaffected.",
    "regression": "Purely additive tests under a new tests/security/ package; the full suite grows from 728 to 868 passed (+140) with 63 skipped unchanged and web vitest still 103 - no existing test weakened or removed.",
    "supply_chain": "No new npm or Python dependency; pip-audit + npm audit (--omit=dev, 0 vulns) + detect-secrets stay green."
  },
  "required_evaluation_level": 2,
  "contract_review": {
    "required": true,
    "reviewer": "owner",
    "disposition": "approved",
    "notes": ["Orchestrator directed F007 Slice 5a: a permanent, cross-cutting, fully-offline negative-security adversarial regression corpus under tests/security/ consolidating + extending the security invariants of the four merged F007 slices. Tests-only (no genuine source gap found). Hard constraints: no migration (head 0009), no new runtime/test dependency, no Redis, no real network, no secrets committed. F007 passes stays false; do NOT merge; do NOT flip passes; then STOP for a fresh-context Level 2 evaluation."]
  }
}
